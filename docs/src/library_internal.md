# Internal Library

## Formulation Function
use `@autodocs` block to splice the docstrings
```@autodocs
Modules = [EGRIP]
Pages   = ["bs_initial_step.jl"]
Order   = [:type, :function]
Private  = true
```


## I/O Function
use `@autodocs` block to splice the docstrings
```@autodocs
Modules = [EGRIP]
Pages   = ["parser.jl"]
Order   = [:type, :function]
Private  = true
```
